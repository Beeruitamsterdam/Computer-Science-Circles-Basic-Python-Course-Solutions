# Choose one of the following options, all three work but just one accepted
print("A double-quote\'s escaped using a backslash, e.g. \\\"")
print('A double-quote\'s escaped using a backslash, e.g. \\"')
print("A double-quote's escaped using a backslash, e.g. \\\"")
